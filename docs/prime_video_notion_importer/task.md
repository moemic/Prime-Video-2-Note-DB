# タスクリスト (既存日付優先機能の追加)

- [x] 実装計画の作成と承認
- [x] `content.js` に監督名（Director）の抽出ロジックを追加
- [x] `popup.js` で抽出された監督名の受け取りと登録日の生成ロジックを追加
- [x] `background.js` で「著者」と「日付」プロパティをNotion APIに送るように修正
- [x] 既存データ読み込み時に「著者」を復元するロジックの追加
- [x] 既存データ読み込み時に「日付」も復元し、更新時は既存日付を優先するロジックの追加
- [x] 動作確認と微調整
- [x] 最後にGitバックアップを実行
