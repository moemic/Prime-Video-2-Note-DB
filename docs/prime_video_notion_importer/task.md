# タスクリスト (ステータス同期機能の追加)

- [x] 実装計画の作成と承認
- [x] `content.js` の修正 (完了済み)
- [x] `background.js` に「著者」「日付」プロパティの保存ロジックを追加 (完了済み)
- [x] `popup.js` に「著者」「日付」の管理ロジックを追加 (完了済み)
- [x] 既存データ読み込み時に「著者」「日付」を復元するロジックを追加 (完了済み)
- [x] `background.js` にステータス候補の取得ロジックを追加
- [x] `popup.html` にステータス選択用のUIを追加
- [x] `popup.js` でステータス候補の取得と選択内容の送信ロジックを実装
- [x] 動作確認（ステータスがNotionと同期されるか）
- [x] 最後にGitバックアップを実行

# タスクリスト (ステータスの新規追加対応)

- [x] 実装計画の作成と承認
- [x] ステータス入力UIの改善（プルダウンから入力可能なコンボボックス形式への検討）
- [x] `background.js` の保存ロジックの調整（Select型への対応など）
- [x] 動作確認
- [x] 最後にGitバックアップを実行

# タスクリスト (ステータスUIのチップ形式化)

- [x] 実装計画の作成と承認
- [x] `popup.html` のステータスUIをチップ・サジェスト形式に変更
- [x] `popup.js` にステータスの選択・表示ロジック（タグと同様）を実装
- [x] 動作確認（単一選択としての挙動維持とUIの整合性）
- [x] 最後にGitバックアップを実行
