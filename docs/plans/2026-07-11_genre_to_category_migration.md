# ジャンル廃止・カテゴリ統合計画

既存ページの `ジャンル` と `カテゴリ` を重複排除して統合し、全件成功後だけ `ジャンル` プロパティを削除する。拡張機能では旧Tags欄を廃止し、タイトル直下のカテゴリ欄へ一本化する。移行は設定画面の明示的なボタンからのみ実行する。

## 検証

- 全ページ更新中に失敗した場合はジャンルを削除しない
- 新規保存・更新ではカテゴリのみ利用する
- 構文、既存回帰、バージョン同期を確認する
